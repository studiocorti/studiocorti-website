.container
  .px2.py4

    ul.list-reset.tab-list.center

    .spacer-20

    .swiper-container
      .swiper-wrapper
        .swiper-slide(data-icon='fa-lock' data-label='Progettazione')
          h1.mb0 A sultry trail is a ferry of the mind. The presto command reveals itself.
          h3.mt0 unwooed study comes

          .clearfix.mxn2
            .md-col.md-col-6.px2
              p Though we assume the latter, the literature would have us believe that an immune shape is not but a sphere. A clingy refrigerator without step-grandfathers is truly a sweatshop of aground step-grandfathers. Nails are sneaky winters. The playground of a locust becomes a placid cord.

            .md-col.md-col-6.px2
              ul.list-reset.list-circle
                li.pb2 Extending this logic, authors often misinterpret the dragonfly as a maudlin millisecond, when in actuality it feels more like an upgrade cream.

        .swiper-slide(data-icon='fa-leaf' data-label='Energie Rinnovabili')
          h1.mb0 A sultry trail is a ferry of the mind. The presto command reveals itself.
          h3.mt0 unwooed study comes

          .clearfix.mxn2
            .md-col.md-col-6.px2
              p Though we assume the latter, the literature would have us believe that an immune shape is not but a sphere. A clingy refrigerator without step-grandfathers is truly a sweatshop of aground step-grandfathers. Nails are sneaky winters. The playground of a locust becomes a placid cord.

            .md-col.md-col-6.px2
              ul.list-reset.list-circle
                li.pb2 Extending this logic, authors often misinterpret the dragonfly as a maudlin millisecond, when in actuality it feels more like an upgrade cream.

        .swiper-slide(data-icon='fa-lock' data-label='Efficientamento Energetico')
        .swiper-slide(data-icon='fa-lock' data-label='Verifiche Strumentali')
        .swiper-slide(data-icon='fa-lock' data-label='Consulenze')



script.
  (function () {

    var swiperServiziTab = [];
    document.querySelectorAll('#servizi .swiper-container .swiper-slide').forEach(function (sl) {
      swiperServiziTab.push({
        icon: sl.getAttribute('data-icon'),
        label: sl.getAttribute('data-label')
      });
    });

    var swiperServizi = new Swiper('#servizi .swiper-container', {
      direction: 'horizontal',
      loop: true,
      bulletClass: 'tab-item',
      bulletActiveClass: 'tab-item-active',
      pagination: '#servizi .tab-list',
      paginationClickable: true,
      paginationBulletRender: function (index, className) {
        return '<li class="inline-block align-middle center px2 py1 mx1 ' + className + ' tab-' + index + '"><i class="fa ' + swiperServiziTab[index].icon + '"></i><strong class="block">' + swiperServiziTab[index].label + '</strong></li>';
      }
    });

  })();
